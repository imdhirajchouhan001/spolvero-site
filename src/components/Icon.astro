---
import { getIcon } from "@/lib/icons";

interface Props {
  name: string;
  class?: string;
}
const { name, class: className = "size-5" } = Astro.props;
const Cmp = getIcon(name);
---

<Cmp className={className} aria-hidden="true" />
